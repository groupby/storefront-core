import Alias from '../../../../src/core/tag/alias';
import suite from '../../_suite';

suite('Tag Alias', () => {

  describe.skip('parse()', () => {
    it('should separate tokens', () => {
      // const data = Alias.parse('my-tag:thing:other:else:what[:inner[stuff]]');
      //
      // expect(data).to.eql({
      //   name: 'my-tag',
      //   flags: ['thing', 'other', 'else', 'what'],
      //   child: {
      //     flags: ['inner'],
      //     child: {
      //       name: 'stuff',
      //       flags: []
      //     }
      //   }
      // });
    });
  });
});
